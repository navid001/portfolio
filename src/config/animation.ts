// Loader configuration.
// The status-boot loader is active on every page by default.
// To disable entirely on a specific page, don't render <PageLoader /> in that page's JSX.

export const LOADER_ENABLED = true;
